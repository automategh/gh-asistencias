{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "meetings": {
      ".indexOn": ["createdBy", "startTime", "status"],
      "$meetingId": {
        ".read": "auth != null",
        ".write": "auth != null && ((data.exists() ? data.child('createdBy').val() === auth.uid : newData.child('createdBy').val() === auth.uid) || root.child('users/' + auth.uid + '/role').val() === 'Admin')"
      }
    },
    "meetingParticipants": {
      "$meetingId": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null && root.child('meetings/' + $meetingId + '/status').val() != 'closed' && root.child('meetings/' + $meetingId + '/status').val() != 'completed' && (auth.uid === $uid || root.child('meetings/' + $meetingId + '/createdBy').val() === auth.uid || root.child('users/' + auth.uid + '/role').val() === 'Admin')"
        }
      }
    },
    "userMeetings": {
      ".indexOn": ["startTime", "status"],
      "$uid": {
        ".indexOn": ["startTime", "status"],
        "$meetingId": {
          ".read": "auth != null",
          ".write": "auth != null && (auth.uid === $uid || root.child('meetings/' + $meetingId + '/createdBy').val() === auth.uid || root.child('users/' + auth.uid + '/role').val() === 'Admin')"
        }
      }
    },
    "users": {
      ".indexOn": ["name", "email", "role"]
    },
    "roles": {
      ".read": "auth != null",
      ".write": "root.child('users/' + auth.uid + '/role').val() === 'Admin'"
    }
  }
}